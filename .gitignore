# Vendored SDK — shipped inside the distribution zip but regenerated via composer install
modules/gateways/paybridgenp/vendor/

# Root-level composer lockfile + dev vendor (phpunit, etc.)
composer.lock
vendor/

# Build artefacts
dist/
*.zip

# Local WHMCS source for Docker dev loop (never commit)
docker/whmcs-src/
docker/db-data/
# Isolated WHMCS 9.x compatibility stack (licensed source + its DB)
docker/whmcs-src-907/
docker/db-data-907/

# IDE / OS
.idea/
.vscode/
.DS_Store

# PHPUnit result cache
.phpunit.result.cache
.gstack/
